import { FilterHeroesPipe } from './filter-heroes.pipe';

describe('FilterHeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
